const express = require("express");

// Ruta reservada para el modulo Cobro.
// Este archivo aun no se monta en server.js porque el modulo
// sigue pendiente de definicion final de controller y contrato API.
const router = express.Router();

module.exports = router;
